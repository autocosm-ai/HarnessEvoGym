apiVersion: harness-rsi/v1alpha1
kind: EvolutionRecipe
spec:
  population:
    mode: mutualism
    concurrency: { n_branches: 2 }
    budget: { total_budget: 16, beta: 0 }
    peer_sharing: { enabled: true }
    competition: { enabled: false }
  moduleSearch:
    # 软提示由 Updater Prompt 说明；excludeRegions 是 Controller 的硬权限边界。
    authority: strategy-directed
    riskCeiling: l3
    strategy: linear-hill-climb
    excludeRegions: [agent-loop, tool-runtime, model-transport, runtime-wiring]
  checkpointing:
    budgetMilestones: [0, 4, 8, 12, 16]
    branchGenerationMilestones: [2, 4, 6, 8]
    capture: { populationBest: true, branchIncumbents: true, latestAttempts: true }
